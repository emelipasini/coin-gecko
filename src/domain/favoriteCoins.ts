export class favoriteCoin {
    username: string;
    favoriteCoins: string[];

    constructor(username: string, favoriteCoins: string[]) {
        this.username = username;
        this.favoriteCoins = favoriteCoins;
    }
}
